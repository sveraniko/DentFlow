# DentFlow Wiki Patch v2

This archive contains only the updated/additional documentation files produced in the recent waves.

Included groups:
- unified card system package (`docs/16*`) including `16-5_card_runtime_state_and_redis_rules.md`
- admin/reception workdesk docs (`docs/68`, `docs/69`)
- care-commerce docs package (`docs/60`, `docs/shop/*`)

Use it to replace or verify the corresponding files in the repo.
